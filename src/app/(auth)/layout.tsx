// src/app/(auth)/layout.tsx — Auth layout (no sidebar, centered)
// Author: Sudarshan Sonawane

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
